version: '3.8'

services:
  latex:
    build: .
    volumes:
      - ./artifacts:/app/artifacts
    working_dir: /app
    command: >
      sh -c "
      python main.py &&
      cd artifacts &&
      pdflatex -interaction=nonstopmode doc.tex"